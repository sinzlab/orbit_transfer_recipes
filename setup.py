from setuptools import setup

setup(
   name='orbit_transfer_recipes',
   version='0.1dev',
   description='Environment and recipes for experiments on inductive bias transfer',
   author='Arne Nix',
   author_email='arnenix@googlemail.com',
   packages=['orbit_transfer_recipes'],  #same as name
   install_requires=[], #external packages as dependencies
)